version: '3.8'

services:
  # This docker-compose file is now simplified without Elasticsearch
  # The backend now uses direct IMAP connections for email management

networks:
  reachinbox-network:
    driver: bridge
